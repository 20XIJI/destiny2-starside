# 复制成 .env.local 再填。.env.local 已在 .gitignore 里，不入库。
# 审核台与云函数共用这一个令牌：tcb fn deploy 按 cloudbaserc.json 的
# {{env.ADMIN_TOKEN}} 注进函数，tools/review.py 从这里现读。
# 生成：python3 -c "import secrets;print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=
