# design.md

Starside 的视觉与排版规范。新增页面、改样式、定颜色之前照此执行。实现细节与生成器约束见 `CLAUDE.md`。

## 总原则

**整页唯一的饱和色来自游戏自身的编码。**外壳一律去饱和为骨白与石墨发丝线：不给外壳加彩色、不加发光边框、不加投影。

一个颜色出现在页面上，读者要能反推出它为什么是这个颜色。做不到就别给它颜色。

---

## 一、色彩

### 色相归元素所有

七个元素色相（电弧、冰影、虚空、缚丝、烈日、动能、棱镜）是游戏的既有编码，属于内容。只调亮度，不改色相。

不属于任何元素的游戏术语走 `--c-term`，作者自己的话走 `--c-note`。给它们借一个元素色相，「颜色即元素编码」当场失效。

**一条例外：连续量的色阶可以借用色相序列，前提是该页没有元素语义。**血量、耗时这类连续量用单色相亮度阶分辨力不够（实测相邻档 ΔE 只到 16.7，而红→橙→金→黄绿→绿能到 21.0，首尾 88.1）。判据是「读者有没有可能把这里的颜色读成元素编码」：`boss-hp/` 八列里没有一格讲元素、属性或伤害类型，那里的绿不会被读成缚丝。反之，只要页面出现元素概念，色相就仍归元素所有，色阶退回单色相亮度阶。

**例外的八个色值收在 `site.css` 的 `.gen tbody td[data-tier="N"]` 上，但不进 `:root`。**它不是站点色彩语言的一部分——进 `:root` 就等于给全站添了八个渲染色；写在那里只为不让第三个用色阶的页面再抄一遍同样的八个值。生效范围仍由内容决定：只有源稿声明了「色阶：」的列才带 `data-tier`。

三个机制色是例外，因为它们在游戏内本来就是那个颜色：

| token | 用于 | 依据 |
|---|---|---|
| `--c-orb` | 能量球、层数 | 游戏内能量球是金色 |
| `--c-health` | 生命值、治疗 | 生命条是红色 |
| `--ammo-heavy` | 威能弹药 | 威能弹药图标是紫色 |

### 强调靠亮度与字重，不靠新色相

- 行内强调（`em` / `strong`）用 `--bone-hi` + `600`，不占色相。
- 着色文字统一 `font-weight: 600`，不加 `text-shadow`。全页着色处逾千，字重与阴影一齐上阵会让语义色失去分级。
- 注解自成一档：作者的话与游戏内的任何编码无关，走 `--c-note` 暗玫瑰红（与最近的 `--c-health` ΔE 20），字重回到正文。全站只有「注解」这一个叫法，也只有 `.note` 这一个类——它曾兼任行标题里的类别、空格占位与强弱箭头三种活，颜色因此从「这是作者的话」退化成「这不重要」。类别走 `.kind`（`--c-legend` 传说紫，游戏内的装备稀有度即此色），占位走 `.na`（`--bone-faint`），箭头是内容、不着色。
- 限定语降级：`--pvp`（方括号里的 PvP 数值）与 `--unsure`（`[?]` 待测值）共用 `--c-aside`，字重回到正文，`.unsure` 带虚下划线。

### 定颜色按 ΔE 决策

对比度只衡量明暗，衡量不了色相。**以 CIELAB ΔE 为准**：

| ΔE | 判读 |
|---|---|
| ≳ 20 | 一眼可分 |
| 12–20 | 需要并排才分得出 |
| < 12 | 视作同一个颜色 |

两条必须一起看的判据：

1. **方向计入判断。**比正文亮 + `600` 字重读作强调；比正文暗 + `600` 字重读作噪声。同样的 ΔE，方向反了就是反效果。
2. **新色与全色板逐个比。**只跟正文比会漏掉与既有色的碰撞。

定色前在 scratchpad 并排渲染同一段真实内容，看过再落。**覆盖变量时覆盖终值**（`--enemy`、`--el-kinetic`），不覆盖上游的 `--c-term`——`--enemy: var(--c-term)` 在 `:root` 就已求值完毕，改后代的 `--c-term` 不生效，五列会渲染成一模一样。

### 两条着色路径

源稿都是 markdown，着色有两种落法，按源稿的性质选：

- **显式标记**（`artifact-mods/`、`references/docs/`）——源稿里写 `{token|文字}`，token 即 `site.css` `:root` 的语义名。逐处可控，能表达词表推不出来的语义；代价是写稿要标颜色。
- **词表着色**（`armor-sets/`）——源稿是纯中文，由 `GLOSSARY` 逐词自动着色。写稿的人不用想颜色、术语全页自动一致；代价是规则全局生效，改一条影响全页。

两条路共用同一批 token，页面之间的颜色语言不分叉。**同一个术语只能落到同一个 token 上**，由 `tools/check_terms.py` 钉住——`--el-solar` 与 `--deb-solar` 渲染色相同，着成哪个用眼睛看不出来。

词表着色的三条约束：

1. **token 只用既有的**，不为新页面新增渲染色。新页面带来新色相，等于每加一页就重新定义一次色彩语言。
2. **词表归属要有依据**，取自游戏内编码或数据源自己的着色，不凭手感分配。依据写在生成器的词表注释里。
3. **长词排在短词前面**，`能量球` 先于 `能量`、`威能弹药` 先于 `弹药`，否则短词先匹配会把长词切碎。

一次都没命中的词是死配置，生成器当场报错。**不写死每个词的命中数**——源稿要持续编辑，写死会让每次改句子都误报。

不属于任何元素的引号内 buff 名走 `--c-term` 暖沙：给它们借一个元素色相，「颜色即元素编码」当场失效。

### 一处契约

改配色只改一处：`assets/site.css` 的 `:root`，语义 token → 7 个元素色相 + 3 个机制色 + `--c-term`（不属于元素的游戏术语）+ `--c-note`（作者的话）+ `--c-legend`（装备稀有度）。源稿里的 token 直接引用这些名字，生成器不必重跑。

同一处还放着三组不属于内容的槽位，改它们同样不必重跑生成器：

| 组 | token | 用于 |
|---|---|---|
| 外壳叠色 | `--tint-1/2/3`、`--head-fill`、`--row-hover` | 石墨底上一层白，浓度即层级。白叠白不引入色相，不与元素编码争 |
| UI 强调色 | `--accent` | 焦点环、当前 chip、卡片左缘。子页面覆盖这一个变量即换色 |
| 版心 | `--wrap`、`--min` | 见第四节 |

**类名按语义命名，不按颜色命名。**敌人分档写 `.bar-red` / `.bar-orange` / `.bar-yellow`：游戏内的血条本来就是这三色，读者能反推出颜色为什么是这个颜色。**同一个颜色不给第二个名字**——同值的两个类名会让同一个术语在两页写成两个 token，而渲染完全相同、肉眼查不出来。这一条由 `tools/check_terms.py` 钉住。

---

## 二、字体与字距

三个角色，在 `site.css` 的 `--font-*`：

| 变量 | 用于 | 字族 |
|---|---|---|
| `--font-disp` | 显示层、字标、小标签 | Chakra Petch（自托管 latin 子集，24 KB） |
| `--font-cn` | 中文标题 | PingFang |
| `--font-body` | 正文与数字 | 等宽字族 |

`--font-disp` 与 `--font-body` 靠浏览器按字符逐族回退：拉丁与数字取前一族字面，中文自动落到 PingFang。不拆标签。

字距只施加于拉丁与数字。`.sect-label` 这类可能是纯中文的位置用 `.2em` 封顶。

**不用字形表达图形语义。**`⯁`（U+2BC1）在中文字体栈下无字形，`◈`（U+25C8）在 serif 下无字形。档位、装饰一律 CSS 绘制，见 `.rhombi`。

---

## 三、中文排版

**空格不是内容。**中文里的空格是数据源的产物，清掉它不算改内容。四条规矩：

| 位置 | 处理 |
|---|---|
| 中文标点两侧 | 不留空格。标点自带边距 |
| 汉字与汉字之间 | 不留空格。中文不分词 |
| 汉字与数字、拉丁之间 | 留一个空格 |
| 括号、引号内侧 | 紧贴内容 |

标签不算字间隔：空格躲在 `</span>` 后面同样要清掉。

着色只覆盖词本身。空白、句读、换行一律移出着色 span，只剩壳的空 span 直接剥掉。

---

## 四、版心与外壳

**版心按页面类型选，不是全站一个数：**

| 页面类型 | 版心 | 例 |
|---|---|---|
| 并排对照 | 走满 1500px | `artifact-mods/` 三档并排 |
| 连续阅读 | 收到 760px 居中 | `ammo/` |
| 左栏索引 + 阅读栏 | 1060px 居中（250 栏 + 44 沟 + 760 正文） | `armor-sets/` |
| 宽表 | 1060px 居中 | `boss-hp/` 八列首领表、`elements/` 各页 |
| 装备版面 | 1060px 居中 | `builds/` 各套配装页 |
| 卡片目录 | 走满 1500px | 首页、`builds/index.html` |
| 全量宽表 | 走满 1500px | `weapon-frames/` 43 列 |

**一共只有三个数：760 / 1060 / 1500。**页面样式表只写 `:root { --wrap: … }` 挑其中一个，版式在 `site.css`。

**宽表复用 1060px 那一档，不为它另定一个数。**表格本身 `width: auto` 居中，版心只是上限；760px 会把最长的说明句折成三四行、整表跟着变高，而再宽一档两侧留白过多。多一种页面类型不等于多一个宽度数。

**列多到 1060px 装不下时才升到 1500px 那一档，与并排对照共用。**武器框架页 43 列全开约 3000px，默认视图 14 列落在 1500px 内；收到 1060px 会把每行折得极高。仍然不为它新开第四个数。

**每一行都拉满版心的版面走 1060px 那一档。**配装页由九行面板组成，行内的面板按各自
的格数分份额，左右两缘因此在整页上落成两条垂直线——那两条线是这类页面的脊柱，比行内
的密度更要紧。1500px 下一行只有四十来件装备，右侧拖出一截空白，行与行反而对不上。
**别用 `column-count` 排这种版面**：分栏点由浏览器定，每次改内容都会跑，两栏的面板
标题各在一个 x 上，脊柱立不起来。

**格宽随行内格数变，跨行不强求等宽。**脊柱与格宽一致不可兼得：行拉满版心，一行五格
就比一行七格宽。配装页的碎片格 193px、职业格 129px，差 50%，那是「每行拉满」的结果，
不是缺陷。要跨行等宽就得把九行强行统一成同一套列数，与内容打架。**脊柱优先。**

**同一行里的格子必须等宽，而这不会自然发生。**份额按格数分只对「面板没有自己的开销」
成立。面板有内边距与边框，格与格之间还有沟，这些固定开销得先从行宽里扣掉，剩下的
再按份额分——写成 `flex: <份额> 1 calc(<面板开销> + (<格数> - 1) * <沟>)`。直接写
`flex: <份额> 1 0` 会让格子少的面板独自扛下整份开销：配装页的超能面板只有一格，
因此比同一行的技能格窄 13.5px。

**面板的内边距取格间沟那个值。**嵌套的两层框（面板一层、格子一层）之间的留白与格与
格之间的留白同宽，整块才读作一套节奏；两个数不等时，靠边那一列格子看着像被挤到了框上。

**同一份样式表服务几种页面时，版心档位按页面特征限定，不写在 `:root` 上。**`builds/`
一份样式表管配装页、索引页与填表页三种：配装页与填表页是 1060px 的九行版面，索引页
是 1500px 的卡片目录。`:root` 给主档，另一档写成 `body:has(.entries) { --wrap: … }`。

**条目多到要扫读、正文又长到要连续读时，走左栏索引。**左栏钉住条目身份（名称、来源、标签），右栏正文仍收在 760px 的阅读宽度上。左栏 `position: sticky` 停在分节标题下方：套装动辄十几行，名字滑出视野就不知道在读谁了。正文宽度不因为多了一条左栏就放宽——阅读宽度由眼睛决定，不由容器决定。

**站点外壳不跟着正文收窄。**导航条与页脚在所有页面同宽（1500px 容器）。它们是站点的框，不是文章的一部分；跟着正文变窄会让同一个页脚在不同页面上长短不一。收窄只作用于正文与页首。

**表格宽度一律走满版心。**表格是 min-content 约束的，内容超过版心时照旧自己撑开，所以这条读作「统一到版心，除非装不下」。宽度统一之后，表与表之间的差异只剩内容本身——按内容定宽时，同一页里几张同构的表会差出几十像素，读者会以为它们不是一回事。

**同一页里列形相同的几张表，列宽也要对齐。**表宽走满版心只统一了外框，列宽仍按各表的内容各量各的：首领生命值页的突袭表与地牢表首列差 59px，六个元素页的三张职业表差 91px，异域护甲四张表差 73px。滚到下一节要重新找列，读者会以为它们不是一回事。

做法是 `table-layout: fixed` 加写死在 `thead th` 上的列宽：短列取该页同构表里那一列的实测最大宽，正文列不给宽度、吃掉余量。带 `nowrap` 的列必须容得下最长那条，否则文字溢出格子。列形不止一种的页面用「恰好 N 列」的前缀圈定（`.gen:has(thead th:nth-child(N)):not(:has(thead th:nth-child(N+1)))`），不按列序裸下规则——`nth-child` 认的是位置不是含义，会溢到另一种表上。

**格内图连同左右内边距要装得进列宽。**装不下时 `margin: 0 auto` 是超约束的，浏览器把右边距解成负值，整枚图往一侧挪半个内边距，看着像整列歪了。判据是「图宽 + 左右内边距 ≤ 列宽」。

**几张表离得远、同屏看不到两张时不做这件事。**对齐看不见，正文列收窄却是实打实的——武器 PERK 详解六张表分布在七万像素上，首列要容得下最长的框架名，说明列会从 801px 掉到 675px，不值。同理，同一列在各表之间差不到一个汉字宽（约 20px）时也不必动。

区段标签的发丝线拉通容器宽度，不跟着正文的限宽断在半途——限宽加在段落上，不加在分节上。

**页脚不重复导航条已有的入口。**回首页的链接在导航条左上角已经有了，页脚只放归属、免责与数据源。

**表头跟着本列的数据走。**列名右对齐还是左对齐由这一列的正文决定：正文右对齐则列名右对齐，正文左对齐则列名左对齐。`site.css` 给 `.gen thead th` 的居中只是缺省，页面把某列的正文拉离居中就要把列名一起改——列名悬在数据上方偏着一侧，扫读的人得先找一遍列名与列的对应关系。一列里混着两种对齐时（元素页的「冷却与槽位」既有左对齐的冷却文字又有居中的槽位图），列名跟着其中的文字那一种——列名本身是文字，而且各页的多寡不同，按多数取会让同一列的列名在冰影页与电弧页各朝一边。列名那一格装的不是列名而是整句说明时（棱镜页共享技能表的左上角）不受这条约束，它跟着句子左对齐。

**首列行标题一律居中。**`th[scope="row"]` 是这一行的身份、多数还带 `rowspan`，居中在合并块里纵横都齐。这一条写在 `site.css` 里，不靠浏览器对 `<th>` 的默认值；这一列的列名同样居中。

**标题默认左对齐；两侧带对称装饰的标题居中。**`✦ 神器模组 · 凯旋纪念碑 ✦` 这类成对装饰本身就是横幅式排法的信号，左对齐会让右侧那颗装饰停在版心中段。居中只作用于标题，正文与分节照旧左对齐。

---

## 五、布局硬约束

- **不在 sticky 元素的祖先链上开 `overflow`。**一旦祖先成为滚动容器，其内部 `position: sticky` 不再相对视口生效，滚动条也会落到约 20000px 高元素的底部、实际不可达。横向溢出交给页面本身，用 `body { min-width: … }` 表达最小内容宽度。
- **不做窄屏适配。**这是明确取向。不新增断点、不做三档纵向堆叠。
- `:focus-visible` 与 `prefers-reduced-motion` 属于质量底线，保留。`clip-path` 会把 `outline` 连带裁掉，那种位置改用内嵌 `box-shadow` 画焦点环。

---

## 六、改样式的做法

1. 改前先想清楚要验证什么：颜色分不分得开、行长是否舒服、表格宽度是否合适。
2. 在 scratchpad 组装对比页，把同一段**真实内容**按几种方案并排渲染。合成内容看不出问题。
3. headless Chrome 截图，`sips -c H W --cropOffset Y X` 裁到能看清字的比例再判断。缩略图上的颜色差异不可信。
4. 定了再落进 `assets/site.css` 或组件页样式。

截图与断言的具体命令见 `CLAUDE.md` 的《验证》。
